{# templates/admin/commande.html.twig #}

{% extends "base-admin.html.twig" %}

{% block title %}Commande | {{ parent() }}{% endblock %}

{% block content %}

    <div class="container  border-danger border-top border-bottom">
        <nav class="nav-bijoux mt-3">
            <div> 
                <ul class="d-flex justify-content-between">
                    <li><a class="nav-link text-dark" href="/admin">Article</a></li>					
                    <li><a class="nav-link text-dark" href="/commande#commande">Commande</a></li>
                    <li><a class="nav-link text-dark" href="/admin#utilisateur">Utilisateur</a></li>                   
                </ul>
            </div>
        </nav>		
	</div>

   <div class="container admin-page mt-5">
        <div>
            <h5 class="text-primary">Les commandes des clients :</h5>
        </div>

        <div>
               <table class="mt-3 table-client">
                    <tr>                                                  
                    <th>Numero de commande</th>
                    <th>Date</th>
                    <th>Nom du client</th>  
                    <th>Détaille</th>                                             
                    </tr>
                
                {# {% for users in users %} #}
                    <tr>
                        <td>{{ users.id}}</td>
                        <td>{{ users.name}}</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>        
                    </tr>  
                    
                {# {% endfor %} #}  
                </table>
        </div>
   </div>
{% endblock %}